[workspace]
members = [
    "rouman-daemon",
    "rouman-cli",
    "rouman-api",
    "rouman-common",
    "rouman-ebpf", "rouman-agent",
]
resolver = "2"

[profile.release]
lto = true
panic = "abort"

[profile.dev]
panic = "abort"
